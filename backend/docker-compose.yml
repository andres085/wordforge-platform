services:
  db:
    image: 'postgres:14'
    restart: always
    ports:
      - 5432:5432
    environment:
      POSTGRES_PASSWORD: user
      POSTGRES_USER: user
      POSTGRES_DB: wordforge
    container_name: wordforge-db
    volumes:
      - ./postgres:/var/lib/postgresql/data
